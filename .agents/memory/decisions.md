# 주요 설계 결정

## [2026-07-10] 온라인 라이트 버전과 PC 버전 사양의 명시적 격리
- **맥락**: PC 버전과 온라인 라이트 버전 간 코드 공유 과정에서 사양 불일치로 인한 빌드 에러와 UI 오동작 발생.
- **결정**: 두 시스템의 `types.ts`, `AssessmentFlow.tsx`, `geminiService.ts`를 독립적으로 완전히 격리하여 수정하고, 라이트 버전은 6단계 흐름으로만 제한함.
- **근거**: 공유로 인한 결합도가 높아져 한쪽의 수정이 다른 한쪽에 예기치 않은 버그를 일으키는 문제를 방지하기 위함.
